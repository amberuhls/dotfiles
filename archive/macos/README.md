# Archived macOS setup

These files preserve the former macOS installer and shell configuration for
reference. macOS is no longer an active target, and shell startup does not load
this directory. The installer retains historical paths and should not be run
without adapting it.
